//singly linked lists node
class SLLNode {
    constructor (value) {
        this.data = value;
        this.next = null;
    }
}

//SLL itself - the whole list itself
class SLL {
    constructor() {
        this.head = null;
    }

    //Add node to the front of the list
    add_node_to_front(value) {
        var newnode = new SLLNode(value);
        
    }

}
